# 数据清洗笔记本指南

根据 [glue_scripts/1_data_cleansing.py](../glue_scripts/1_data_cleansing.py) 创建的两个本地数据清洗笔记本。

## 📋 笔记本列表

### 1. Pandas 版本（推荐 ⭐⭐⭐）

**文件路径**: `test/pandas/test_pandas_cleansing.ipynb`

**特点**:
- ✓ 无需 Java 依赖
- ✓ 速度快（启动 < 1秒）
- ✓ 完全兼容 Windows
- ✓ 代码易读易维护
- ✓ 包含数据可视化和统计分析

**适用场景**:
- 开发和测试
- 数据分析和可视化
- 单机数据处理（< 100GB）

---

### 2. Spark 版本（⭐⭐）

**文件路径**: `test/spark/test_spark_cleansing.ipynb`

**特点**:
- ✓ 分布式处理
- ✓ 大数据支持
- ✓ 生产级性能
- ✓ 与 AWS Glue 兼容

**适用场景**:
- 生产环境
- 大数据处理（> 100GB）
- 分布式计算

**需求**: Java 11

---

## 🚀 快速开始

### Pandas 版本（推荐）

```bash
# 1. 进入项目目录
cd c:\Users\hy120\Downloads\zhihullm\CASE-customer-group

# 2. 激活虚拟环境
.venv\Scripts\activate.bat

# 3. 安装 Pandas (如果还未安装)
pip install pandas

# 4. 启动 Jupyter
jupyter notebook test/pandas/test_pandas_cleansing.ipynb
```

### Spark 版本

```bash
# 1. 进入项目目录
cd c:\Users\hy120\Downloads\zhihullm\CASE-customer-group

# 2. 激活虚拟环境
.venv\Scripts\activate.bat

# 3. 安装 Java 11 (如果还未安装)
# https://www.oracle.com/java/technologies/downloads/#java11

# 4. 设置 JAVA_HOME
set JAVA_HOME=C:\Program Files\Java\jdk-11

# 5. 启动 Jupyter
jupyter notebook test/spark/test_spark_cleansing.ipynb
```

---

## 📊 笔记本功能对比

| 功能 | Pandas | Spark |
|------|--------|-------|
| 环境配置 | ✓ | ✓ (含Java配置) |
| 数据加载 | ✓ | ✓ |
| 客户基本信息清洗 | ✓ | ✓ |
| 客户行为资产清洗 | ✓ | ✓ |
| 数据质量报告 | ✓ | ✓ |
| 数据输出 | ✓ | ✓ |
| 统计分析 | ✓ | ✗ |
| 数据可视化 | ✓ | ✗ |
| 启动时间 | < 1秒 | 10-20秒 |
| 处理速度 (小数据) | 快 | 中等 |
| 处理速度 (大数据) | 慢/OOM | 快 |

---

## 🔍 数据处理流程

### 第一部分：加载数据

```
输入:
  - customer_base.csv (客户基本信息)
  - customer_behavior_assets.csv (客户行为资产)

输出:
  - df_customer_base
  - df_customer_behavior
```

### 第二部分：清洗客户基本信息

**步骤**:

1. **数据类型转换**
   - 字符串去空格
   - 数值类型转换
   - 日期类型转换

2. **异常值处理**
   - 年龄: 18-100 岁
   - 月收入: 0-100万

3. **数据标准化**
   - 性别: [男, 女]
   - 日期提取: 年月信息

4. **缺失值统计**
   - 记录各字段缺失值

5. **去重**
   - 基于 customer_id
   - 保留第一条记录

### 第三部分：清洗客户行为资产

**步骤**:

1. **数据类型转换**
   - 资产类字段转换为 double
   - 标志字段转换为 int
   - 时间戳转换

2. **资产数据验证**
   - 总资产: 0-1亿
   - 资产结余检查

3. **行为数据验证**
   - 非负数检查
   - 对负数进行修正

4. **产品标志验证**
   - 检查取值必须为 0 或 1

5. **缺失值处理**
   - contact_result 缺失值标记

6. **去重**
   - 基于 (customer_id, stat_month)
   - 保留最新的记录 (按 last_app_login_time)

### 第四部分：输出数据

```
输出目录: output/
  - cleaned_customer_base.csv
  - cleaned_customer_behavior.csv
```

---

## 📊 数据质量报告

笔记本会生成详细的数据质量检查报告，包含：

**客户基本信息**:
- 输入/输出行数
- 移除重复行数
- 年龄异常值
- 收入异常值
- 性别异常值

**客户行为资产**:
- 输入/输出行数
- 移除重复行数
- contact_result 缺失值
- 资产异常值

**示例输出**:
```
========================================
数据质量检查报告
========================================

【客户基本信息】
  input_rows: 10000
  output_rows: 9985
  duplicate_removed: 15
  age_invalid_count: 5
  income_invalid_count: 3
  gender_invalid_count: 0

【客户行为资产】
  input_rows: 15000
  output_rows: 14950
  duplicate_removed: 50
  contact_result_missing: 120
  assets_invalid_count: 2
```

---

## 🔧 核心清洗规则

### 客户基本信息

```
字段             清洗规则
─────────────────────────────────────
customer_id      去空格
name             去空格
age              转int, 异常值(< 18 或 > 100)设为空
gender           验证是否为 [男, 女]
monthly_income   转double, 异常值(< 0 或 > 100万)设为空
open_account_date 转日期, 提取年月
其他字符字段     去空格
去重规则         基于 customer_id
```

### 客户行为资产

```
字段                     清洗规则
──────────────────────────────────────
total_assets            转double, 验证0-1亿范围
各类余额                转double
各类标志                转int, 验证0或1
各类计数                转int
时间戳字段              转timestamp
负数字段                负数改为0
contact_result          标记缺失值
去重规则                基于(customer_id, stat_month),
                        保留最新的记录
```

---

## 🛠 自定义和扩展

### 修改清洗规则

**Pandas 版本** - 修改异常值阈值:
```python
# 原来
df_customer_base_cleaned.loc[
    (df_customer_base_cleaned['age'] < 18) | (df_customer_base_cleaned['age'] > 100),
    'age'
] = np.nan

# 修改为
df_customer_base_cleaned.loc[
    (df_customer_base_cleaned['age'] < 16) | (df_customer_base_cleaned['age'] > 120),
    'age'
] = np.nan
```

**Spark 版本** - 修改异常值阈值:
```python
# 原来
when((col("age") < 18) | (col("age") > 100), None)

# 修改为
when((col("age") < 16) | (col("age") > 120), None)
```

### 添加新的清洗规则

可以在相应的步骤后添加新的数据处理逻辑。例如：

```python
# Pandas
df_customer_base_cleaned['age_group'] = pd.cut(
    df_customer_base_cleaned['age'],
    bins=[0, 30, 40, 50, 100],
    labels=['18-30', '31-40', '41-50', '50+']
)

# Spark
from pyspark.sql.functions import when
df_customer_base_cleaned = df_customer_base_cleaned.withColumn(
    "age_group",
    when(col("age") <= 30, "18-30")
    .when(col("age") <= 40, "31-40")
    .when(col("age") <= 50, "41-50")
    .otherwise("50+")
)
```

---

## ⚠️ 常见问题

### Q: 笔记本无法找到数据文件？

**原因**: 未在项目根目录或 Jupyter 工作目录不正确

**解决**:
```python
# 检查当前目录
from pathlib import Path
print(Path.cwd())

# 确保数据文件存在
import os
print(os.listdir('.'))
```

### Q: Spark 版本报 Java 错误？

**原因**: Java 未安装或版本不兼容

**解决**:
```bash
# 检查 Java
java -version

# 安装 Java 11
# https://www.oracle.com/java/technologies/downloads/#java11

# 设置环境变量
set JAVA_HOME=C:\Program Files\Java\jdk-11
```

### Q: Pandas 版本缺少依赖？

**原因**: NumPy 或 Pandas 未安装

**解决**:
```bash
pip install pandas numpy
```

### Q: 输出文件位置在哪里？

**位置**: `output/` 目录
```
output/
  ├── cleaned_customer_base.csv
  └── cleaned_customer_behavior.csv
```

---

## 📈 性能对比

| 指标 | Pandas | Spark |
|------|--------|-------|
| 启动时间 | < 1秒 | 10-20秒 |
| 处理速度 (小数据) | 快 | 中等 |
| 处理速度 (大数据) | 慢/OOM | 快 |
| 内存使用 | 中等 | 高 |
| 分布式 | ✗ | ✓ |

**建议**:
- 数据 < 10GB: 使用 Pandas
- 数据 > 100GB: 使用 Spark
- 开发阶段: 使用 Pandas
- 生产环境: 使用 Spark 或 AWS Glue

---

## 📝 笔记本结构

两个笔记本都遵循相同的结构，便于理解和维护：

```
1. 环境配置
   └─ 导入库、修复编码、初始化

2. 加载数据
   └─ 读取 CSV、显示信息

3. 清洗客户基本信息表
   ├─ 数据类型转换
   ├─ 异常值处理
   ├─ 数据标准化
   ├─ 缺失值统计
   └─ 去重

4. 清洗客户行为资产表
   ├─ 数据类型转换
   ├─ 资产数据验证
   ├─ 行为数据验证
   ├─ 产品标志验证
   ├─ 缺失值处理
   └─ 去重

5. 数据质量检查报告
   └─ 生成详细报告

6. 输出清洗后的数据
   └─ 保存 CSV 文件

7. 清洗结果统计
   └─ 显示保留率等统计

8. 清洗结果预览
   └─ 显示前几行数据

9. 数据分析 (Pandas 版本)
   └─ 可视化分析结果
```

---

## 🔗 与 AWS Glue 脚本的对应关系

两个笔记本完全基于 [glue_scripts/1_data_cleansing.py](../glue_scripts/1_data_cleansing.py) 实现：

| Glue 脚本部分 | Pandas 笔记本 | Spark 笔记本 |
|------|------|------|
| 导入库 | 第1、2步 | 第1、2步 |
| 加载数据 | 第2步 | 第2步 |
| 清洗客户基本信息 | 第3步 | 第3步 |
| 清洗客户行为资产 | 第4步 | 第4步 |
| 数据质量检查 | 第5步 | 第5步 |
| 输出数据 | 第6步 | 第6步 |

---

## 💡 关键特性

### 1. 自动路径检测

两个笔记本都能自动检测项目根目录：

```python
project_root = Path.cwd()
if project_root.name != "CASE-customer-group":
    project_root = project_root.parent
```

### 2. 详细的进度提示

```
✓ 数据加载完成
✓ 数据类型转换完成
✓ 异常值处理完成
✓ 去重完成
```

### 3. 完整的质量报告

包含输入/输出行数、异常值统计、保留率等信息。

### 4. UTF-8 编码支持

正确处理中文字符，支持 Windows 系统。

---

## 🔗 相关资源

- [AWS Glue 清洗脚本](../glue_scripts/1_data_cleansing.py)
- [Pandas 文档](https://pandas.pydata.org/)
- [PySpark 文档](https://spark.apache.org/docs/latest/api/python/)
- [Jupyter 文档](https://jupyter.org/)

---

## ✅ 检查清单

- [x] 创建 test/pandas/ 目录
- [x] 创建 test/spark/ 目录
- [x] 实现 Pandas 版本笔记本
- [x] 实现 Spark 版本笔记本
- [x] 创建使用指南
- [x] 验证笔记本文件有效性
- [x] 添加代码注释和说明

---

## 🎉 立即开始

### 使用 Pandas（推荐）

```bash
jupyter notebook test/pandas/test_pandas_cleansing.ipynb
```

### 使用 Spark

```bash
set JAVA_HOME=C:\Program Files\Java\jdk-11
jupyter notebook test/spark/test_spark_cleansing.ipynb
```

**祝您使用愉快！** 🚀

---

**创建于**: 2025-12-03
**基于**: glue_scripts/1_data_cleansing.py
**版本**: 1.0
