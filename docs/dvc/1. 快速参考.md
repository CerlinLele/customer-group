# DVC 快速参考指南

## 🚀 快速启动 (已完成)

```bash
# 1. 安装 DVC
pip install dvc==3.64.0

# 2. 初始化 DVC
dvc init

# 3. 配置本地存储
dvc remote add -d storage .dvc-storage

# 4. 追踪数据文件
dvc add customer_base.csv
dvc add customer_behavior_assets.csv

# 5. 推送数据
dvc push
```

---

## 📋 日常操作命令

### 数据管理
| 操作 | 命令 |
|------|------|
| 追踪新文件 | `dvc add <file>` |
| 查看状态 | `dvc status` |
| 推送数据 | `dvc push` |
| 拉取数据 | `dvc pull` |
| 恢复文件 | `dvc checkout` |
| 删除追踪 | `dvc remove <file>.dvc` |

### 流水线操作
| 操作 | 命令 |
|------|------|
| 运行流水线 | `dvc repro` |
| 查看流水线图 | `dvc dag` |
| 查看输出文件 | `dvc status` |
| 修改参数重新运行 | `dvc repro --single-stage <stage>` |

### 实验管理
| 操作 | 命令 |
|------|------|
| 运行实验 | `dvc exp run --name exp-001` |
| 查看实验 | `dvc exp show` |
| 比较实验 | `dvc exp diff` |
| 应用实验 | `dvc exp apply <exp-id>` |

### 指标和参数
| 操作 | 命令 |
|------|------|
| 查看指标 | `dvc metrics show` |
| 比较指标 | `dvc metrics diff` |
| 查看参数 | `dvc params show` |
| 比较参数 | `dvc params diff` |

---

## 🔗 Git + DVC 协作流程

### 个人开发
```bash
# 1. 获取最新代码和数据
git pull
dvc pull

# 2. 创建实验分支
git checkout -b experiment/my-feature

# 3. 修改代码/参数
vim params.yaml

# 4. 运行流水线
dvc repro

# 5. 查看结果
cat outputs/metrics/model_metrics.json

# 6. 提交更改
dvc push
git add .
git commit -m "feat: improve model performance"
git push
```

### 团队协作 (拉取请求)
```bash
# 队友更新了数据
git pull
dvc pull

# 生成新特征
python scripts/feature_engineering.py
dvc add data/features/new_features.csv
dvc push

# 提交到 Git
git add data/features/new_features.csv.dvc .gitignore
git commit -m "feat: add new features"
git push

# 创建 Pull Request
gh pr create --title "Add new features"
```

---

## 🎯 本项目配置

### 项目信息
- **项目名**: 客户分群精准营销 MLOps
- **DVC 版本**: 3.64.0
- **Python 版本**: 3.10+
- **虚拟环境**: `.venv/`

### 存储配置
- **远程存储**: `.dvc-storage/` (项目内本地存储)
- **存储类型**: 本地文件系统 (开发测试)
- **数据总量**: 26.7MB

### 追踪的文件
- ✅ `customer_base.csv` (1.7MB)
- ✅ `customer_behavior_assets.csv` (25MB)

---

## 🐛 常见问题

### Q1: 如何删除已追踪的文件？
```bash
# 方式1: 保留文件，仅删除 DVC 追踪
dvc remove customer_base.csv.dvc
git add customer_base.csv.dvc.gitignore
git commit -m "Remove DVC tracking"

# 方式2: 同时删除文件
dvc remove --outs customer_base.csv.dvc
```

### Q2: 如何修改远程存储地址？
```bash
# 修改存储地址
dvc remote modify storage url D:\new-storage-path

# 重新推送数据
dvc push
```

### Q3: 如何在其他电脑上恢复数据？
```bash
# 在新电脑上
git clone <repo-url>
cd CASE-customer-group
dvc pull  # 自动从 .dvc-storage 恢复数据
```

### Q4: 如何查看数据变化历史？
```bash
# 查看 .dvc 文件的 Git 历史
git log --oneline customer_base.csv.dvc

# 查看每个版本的哈希值
git show <commit>:customer_base.csv.dvc
```

### Q5: 如何清理本地缓存？
```bash
# 显示缓存大小
dvc cache dir

# 清理未使用的缓存
dvc cache remove --not-in-remote
```

---

## 📊 工作流总结

```
┌─────────────────┐
│   修改代码      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  修改 params.yaml│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  运行 dvc repro  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  查看结果       │
│ (metrics.json)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  dvc push       │ (推送数据)
│  git commit     │ (提交代码)
│  git push       │ (推送代码)
└─────────────────┘
```

---

## ✨ 下一步行动

1. **提交配置文件到 Git**
   ```bash
   git add .dvc .dvcignore .gitignore customer_*.csv.dvc requirements.txt
   git commit -m "chore: Initialize DVC with local storage"
   ```

2. **创建项目目录结构** (第2周)
   - `data/raw/` - 原始数据
   - `data/processed/` - 处理后数据
   - `models/` - Python 模块
   - `scripts/` - 执行脚本
   - `outputs/` - 模型输出

3. **开始构建流水线** (第2-3周)
   - 创建 `params.yaml` 参数文件
   - 创建 `dvc.yaml` 流水线定义
   - 实现数据验证和预处理阶段

---

**📞 需要帮助？查看 [DVC初始化配置.md](1.\ DVC初始化配置.md) 获取详细说明。**
**📖 查看 [README.md](1.\ README.md) 了解文档导航。**
**🎯 查看 [DVC实施计划.md](0.\ DVC实施计划.md) 了解项目路线图。**
